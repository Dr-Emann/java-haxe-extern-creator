apply plugin: 'java'

repositories
{
	flatDir {
        dirs 'lib'
    }
}

dependencies
{
	compile name: 'jCFL-2.0.161'
	compile files(org.gradle.internal.jvm.Jvm.current().toolsJar)
  
}

sourceSets 
{
    main {
        java {
            srcDir 'src'
        }        
    }
}